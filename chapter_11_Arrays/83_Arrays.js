//Arrays are used to store multiple values in a single variable.
//Arrays are indexed based.
//Array is an collection of similar data types and elements.

let fruits = [];

let fruits_fresh = ["apple", "banana", "cherry", "date", "elderberry"];
//length of the array is 5 and index starts from 0 to 4

let arr = [10, 20, 30, 40, 50];
//length of the array is 5 and index starts from 0 to 4

console.log(arr[0]); //10
console.log(arr[1]); //20
console.log(arr[5]); //undefined

let testsresults = ["pass", "fail", "skip"];
console.log(testsresults);

let mixedarray = ["apple", 10, true, "banana", 20, false];
//mixed array is an array that contains different data types and elements.
console.log(mixedarray);
