//single quotes

let single = "Hello Word";
let withDouble = 'She said "Hello"';

//double quotes
let double = "Hello World";

// javascript does not have a character type. Instead, a single character is represented as a string of length 1.

let c = "c";
let c1 = "cc";
console.log(c.length); //1
console.log(typeof c); //string
console.log(typeof c1); //string
console.log(typeof double); //string

//JavaScript prefer to use single quotes for strings.
