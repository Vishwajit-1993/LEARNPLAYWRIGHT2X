//Function can be stored in a variable

const greet = function (name) {
  return `Hello , ${name}`;
};
console.log(greet()); //output: Hello,undefined
console.log(greet("Vishwajit")); //output: Hello,Vishwajit
